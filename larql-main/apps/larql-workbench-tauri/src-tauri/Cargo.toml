[package]
name = "larql-workbench-tauri-shell"
version = "0.1.0"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2.0", features = [] }

[dependencies]
serde = { version = "1.0", features = ["derive"] }
tauri = { version = "2.0", features = [] }
larql-workbench-core = { path = "../../../crates/larql-workbench-core" }

[workspace]
